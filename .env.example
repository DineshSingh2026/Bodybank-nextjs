# When developing: run the existing BodyBank Express server (e.g. on :3000),
# then point this Next app at it so /api/* and /uploads/* proxy correctly.
#
# Example:
# LEGACY_ORIGIN=http://localhost:3000

LEGACY_ORIGIN=
